{
  "recommendations": [
    // Autocompletado y comprobacion de tipos dentro de las plantillas
    "angular.ng-template",
    // El linter hace ejecutable la regla de capas: conviene verla al escribir,
    // no al ejecutar npm run lint
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode"
  ]
}
